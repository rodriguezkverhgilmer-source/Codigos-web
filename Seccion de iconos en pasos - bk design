{% schema %}
{
  "name": "Icons Section",
  "tag": "section",
  "class": "icons-section",
  "settings": [
    {
      "type": "color",
      "id": "background_color",
      "label": "Background color",
      "default": "#F5F5F5"
    },
    {
      "type": "text",
      "id": "title",
      "label": "Title",
      "default": "From the factory to your home"
    },
    {
      "type": "textarea",
      "id": "description",
      "label": "Description",
      "default": "We eliminate intermediaries and deliver directly from our factory to your home."
    }
  ],
  "blocks": [
    {
      "type": "step",
      "name": "Step",
      "settings": [
        {
          "type": "image_picker",
          "id": "icon",
          "label": "Icon image"
        },
        {
          "type": "text",
          "id": "label",
          "label": "Step label",
          "default": "Made in Spain"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Icons Section",
      "category": "Custom",
      "blocks": [
        { "type": "step" },
        { "type": "step" },
        { "type": "step" }
      ]
    }
  ]
}
{% endschema %}

<section class="icons-section-wrapper" style="background-color: {{ section.settings.background_color }};">
  <div class="icons-section-content">
    {% if section.settings.title != blank %}
      <h2 class="icons-section-title">{{ section.settings.title }}</h2>
    {% endif %}
    {% if section.settings.description != blank %}
      <p class="icons-section-description">{{ section.settings.description }}</p>
    {% endif %}

    <div class="icons-section-steps">
      {% for block in section.blocks %}
        <div class="step-item">
          {% if block.settings.icon %}
            <img
              src="{{ block.settings.icon | image_url: width: 100 }}"
              srcset="{{ block.settings.icon | image_url: width: 200 }} 2x"
              alt="{{ block.settings.label }}"
              loading="lazy"
              width="100"
              height="100"
            >
          {% endif %}
          <p class="step-label">{{ block.settings.label }}</p>
        </div>

        {% unless forloop.last %}
          <div class="step-arrow">→</div>
        {% endunless %}
      {% endfor %}
    </div>
  </div>
</section>

<style>
  .icons-section-wrapper {
    padding: 20px 20px;
  }

  .icons-section-content {
    max-width: 1000px;
    margin: 0 auto;
    text-align: center;
  }

  .icons-section-title {
    font-size: 22px;
    font-weight: normal;
    margin-bottom: 12px;
    color: #111;
  }

  .icons-section-description {
    font-size: 16px;
    color: #111;
    margin-top: 0px;
    margin-bottom: 40px;
  }

  .icons-section-steps {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    align-items: center;
    gap: 10px;
  }

  .step-item {
    display: flex;
    flex-direction: column;
    align-items: center;
    max-width: 150px;
  }

  .step-item img {
    width: 100px;
    height: auto;
    margin-bottom: 10px;
  }

  .step-label {
    font-size: 14px;
    font-weight: normal;
    color: #222;
    text-align: center;
  }

  .step-arrow {
    font-size: 24px;
    font-weight: bold;
    margin: 0px;
    color: #d07347;
  }

  @media screen and (max-width: 768px) {
    .icons-section-steps {
      flex-wrap: nowrap;
      overflow-x: auto;
      justify-content: flex-start;
      gap: 0px;
      padding-bottom: 10px;
      -webkit-overflow-scrolling: touch;
      scroll-snap-type: x mandatory;
      scrollbar-width: none;
    }

    .icons-section-steps::-webkit-scrollbar {
      display: none;
    }

    .step-item,
    .step-arrow {
      flex: 0 0 auto;
      min-width: 150px;
      max-width: 170px;
      scroll-snap-align: center;
    }

    .step-label {
      padding: 0px;
    }

    .step-arrow {
      transform: none;
      margin: 0 px;
    }
  }
</style>

